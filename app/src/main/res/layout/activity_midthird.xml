<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:id="@+id/midthird_editText"
        android:paddingLeft="20dp"
        android:paddingRight="90dp"
        android:background="@drawable/commentbox" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/commentbutton"
        android:id="@+id/midthird_imageButton"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginRight="20dp"
        android:layout_marginBottom="25dp"
        android:background="#000000FF"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="400dp"
        android:scrollbars="vertical"
        android:layout_above = "@id/midthird_editText"
        android:id="@+id/midthird_textView"
        android:padding="20dp"
        android:gravity="left"
        android:background="@drawable/fullcommentbox"/>

</RelativeLayout>